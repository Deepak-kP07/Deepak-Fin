<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Native Google sign-in redirect (see lib/auth/nativeGoogleAuth.js) — Google
                 Identity Services doesn't work inside an embedded WebView, so native sign-in
                 goes through Supabase's OAuth redirect flow instead, opened in a Chrome Custom
                 Tab; this is what hands control back to the app once it completes. Must match
                 REDIRECT_URL in that file, and be added to Supabase's Authentication > URL
                 Configuration > Redirect URLs allowlist. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.personalfin.app" android:host="login-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- SMS Auto-Detect (see android/app/src/main/java/com/personalfin/app/sms). Not exported
             — only the system is meant to deliver SMS_RECEIVED to this app, never another app. -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="500">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Without these, FCM falls back to a generic system icon/tint for any notification-
             payload push it auto-displays (background/closed app) — see
             res/drawable-*dpi/ic_stat_notification.png (a white silhouette; Android requires
             alpha-only icons here, a full-color launcher icon can't be used directly) and
             res/values/notification_colors.xml for the accent color. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent_color" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- SMS Auto-Detect — both are runtime-dangerous permissions requested explicitly through the
         in-app onboarding screen (never on launch without explanation first), never assumed. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
</manifest>
